<% content_for :altnav do %>
  <%= link_to "edit profile", profile_edit_path if @auth == @profile %>
  <%= link_to "send message", new_message_path(@profile) if @auth != @profile %>
<% end %>

<h1>profile</h1>
<%= render :partial => 'user', :locals => {:profile => @profile} %>
